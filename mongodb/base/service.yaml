apiVersion: v1
kind: Service
metadata:
  name: mongodb
  labels:
    app.kubernetes.io/name: mongodb
    app.kubernetes.io/part-of: mongodb
spec:
  selector:
    app.kubernetes.io/name: mongodb
#  type: NodePort
  ports:
    - name: mongodb
      port: 27017
      targetPort: mongodb
#      nodePort: 30017
